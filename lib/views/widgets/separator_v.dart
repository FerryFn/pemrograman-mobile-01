import 'package:flutter/material.dart';

class SeparatorV extends StatelessWidget {
  const SeparatorV({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24,
    );
  }
}